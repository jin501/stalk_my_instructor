require 'spec_helper'

describe StalkMyInstructor do
  it 'has a version number' do
    expect(StalkMyInstructor::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
